{
  "name": "polysmith",
  "private": true,
  "author": "Stefan Ovezea <sovezea@gmail.com>",
  "license": "AGPL-3.0-or-later",
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "node scripts/kill-dev-port.mjs 1420 && pnpm --filter desktop-ui tauri dev",
    "ui:dev": "pnpm --filter desktop-ui dev",
    "ui:build": "pnpm --filter desktop-ui build",
    "ui:install": "pnpm install",
    "build:release": "node scripts/build-release.mjs",
    "core:configure": "node scripts/configure-core.mjs",
    "core:build": "node scripts/build-core.mjs",
    "core:rebuild": "pnpm core:configure && pnpm core:build",
    "test:core": "node scripts/run-core-tests.mjs",
    "test:ai": "pnpm --filter desktop-ui exec vitest run src/lib/ai/aiSketchGeneration.integration.test.ts src/lib/ai/cadCoreHarness.smoke.test.ts",
    "test": "pnpm test:core",
    "deps:sync": "git submodule update --init --recursive",
    "occt:configure": "node scripts/configure-occt.mjs",
    "occt:build": "cmake --build third_party/occt8-build --config Release --parallel 2",
    "occt:install": "cmake --install third_party/occt8-build --config Release",
    "occt:rebuild": "pnpm deps:sync && pnpm occt:configure && pnpm occt:build && pnpm occt:install",
    "bootstrap": "pnpm deps:sync && pnpm install && pnpm occt:configure && pnpm occt:build && pnpm occt:install && pnpm core:configure && pnpm core:build"
  }
}
