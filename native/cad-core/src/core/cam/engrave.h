#pragma once

namespace polysmith::core {

// Registers the "engrave" toolpath generator (mill engrave — the
// on-line sketch trace at a fixed depth).
void register_engrave_generator();

}  // namespace polysmith::core
